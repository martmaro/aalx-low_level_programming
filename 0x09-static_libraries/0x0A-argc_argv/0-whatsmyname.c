#include <stdio.h>
#include "main.h"

/**
 * main - prints the number of the program
 * @argc: number of arguments
 * argv: array of arguments
 * Return: Always 0
 */
int main(int argc _attribute_((unused)), char *argve[])
{
	printf("%s\n", *argv);
	return (0);
}
